# acrux-app

Application native Acrux (fenêtrage, interface, outils)

Voir `ARCHITECTURE.md` a la racine pour la place de ce crate dans l ensemble,
et `src/lib.rs` pour la liste des modules prevus.

Tests : `cargo test -p acrux-app`
